{
  "//": "Annotates javac diagnostics on PRs — including Error Prone / NullAway, which print as javac error:/warning: lines. Registered with ::add-matcher:: before the Gradle build; the build itself is the gate (Error Prone errors fail compilation).",
  "problemMatcher": [
    {
      "owner": "javac-errorprone",
      "pattern": [
        {
          "regexp": "^(.*\\.java):(\\d+):\\s+(error|warning):\\s+(.*)$",
          "file": 1,
          "line": 2,
          "severity": 3,
          "message": 4
        }
      ]
    }
  ]
}
